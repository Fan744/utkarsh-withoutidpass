services:
  - type: worker
    name: utkarsh-telegram-bot
    runtime: python
    region: singapore
    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: |
      python bot.py

    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
